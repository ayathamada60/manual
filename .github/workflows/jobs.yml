 on:
  workflow_disbatch:

jobs: 
  job_1:
    runs-on: ubuntu:latest
    steps:
    - runs: "echo job ${{ github.job }}"

    
job_2:
  runs-on: ubuntu:latest
  needs: job_1
  steps:
   - runs: "echo job ${{ github.job }}"

   
job_3:
  runs-on: ubuntu:latest 
  needs: job_1
  steps:
  - runs: "echo job ${{ github.job }}"

   
job_4:
  runs-on: ubuntu:latest
  needs: [job_2,job_3]
  steps:
  - runs: "echo job ${{ github.job }}"
